Which of the following expressions will evaluate to true if preceded by the following code?Which of the following expressions will evaluate to true
if preceded by the following code?

(a == d) //true

(b == d)
//b and d can not even be compared because they are of different types.

(a == "java") //true

a.equals(c) //true
//Note that a == c will be false because doing 'new' creates an entirely new object.
