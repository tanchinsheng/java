Which of the following comparisons will yield false

Boolean.parseBoolean("true") == true

Boolean.parseBoolean("TrUe") == new Boolean(null); //true
//This will yield false because parseBoolean("TrUe") will return true and
new Boolean(null) will return a Boolean wrapper object containing false.

new Boolean("TrUe") == new Boolean(true); //true
//Even though both the sides have a Boolean wrapper containing true,
the expression will yield false because they point to two different
Boolean wrapper objects.

new Boolean() == false;
//This will not compile because Boolean class does not have a no-args constructor.

new Boolean("true") == Boolean.TRUE
//Even though both the sides have a Boolean wrapper containing true,
the expression will yield false because they point to two different
Boolean wrapper objects.

new Boolean("no") == false;
//Any string other than "true" (ignoring case) will produce a Boolean containing false.
Therefore, this expression will yield true.

