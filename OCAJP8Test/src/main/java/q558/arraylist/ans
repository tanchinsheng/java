It will not compile.

It will throw an exception at run time because of line //1

It will throw an exception at run time because of line //2
//Although 111 is a primitive, it will automatically be boxed into an Integer object.
So there will be no exception because of this.

It will throw an exception at run time because of line //3
//It will throw an IndexOutOfBoundsException at run time because of this line.
The size() method of ArrayList returns the number of elements. Here, it returns 1.
Since numbering in ArrayList starts with 0. al.get(1) will cause an IndexOutOfBoundsException to be thrown
because only 0 is a valid index for a list of size 1.

null
