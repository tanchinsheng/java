What will the following program print?

It will not compile.

It will compile but throw an exception at runtime.

It will print 'null' (without quotes). //true

It will print nothing.

None of the above.

Explanation
All member fields (static and non-static) are initialized to their default values.
Objects are initialized to null (String is also an object), numeric types to
0 (or 0.0 ) and boolean to false.

