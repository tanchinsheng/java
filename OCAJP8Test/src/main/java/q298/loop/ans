What will the following code print when compiled and run:

1

1
0

2
1

2
1
0

It will keeping printing numbers in an infinite loop.

It will not compile.//true

Explanation
In Java, a while or do/while construct takes an expression that returns a boolean.
The expression --k is an integer, which is invalid and so the compilation fails.
You could change it to: while( --k>0 ){ ... }.
In this case, --k<0 is a boolean expression and is valid.
