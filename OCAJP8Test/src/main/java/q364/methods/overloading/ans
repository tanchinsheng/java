What will be the output?

It will fail to compile.

m1 int

m1 double//true

m1 String

Explanation

Here, m1() is overloading for three different argument types.
So when you call ot.m1(1.0), the one with argument of type double will be invoked.
